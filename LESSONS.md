# LESSONS.md — what reached a review round, and what now carries it

One row per class of correctness finding a review round reported and a fix
commit closed. A wording or record finding is not a lesson; a wrong output,
an unpinned rule, an unshaped input, a mechanism in the wrong place is. The
fix commit appends the class's row or extends its Where cell (CLAUDE.md →
Workflow rules, "Fix commits"). The row is read before code is written, not
after: `/phase-start` prints the `open` rows, `/preflight` asks each one of
every changed symbol, and `code-craft` points here.

**Promotion.** A class hit twice becomes a mechanism — a ruff rule, a test, a
guard script, or one sentence in a standard — and its Status says which:
`promoted → <mechanism>`. A promoted class that recurs reopens its row: the
mechanism failed, so the mechanism is reworked, not the row. A row `open` for
two phases with no second hit is closed as `expired <date>`. Without this rule
the file grows into a list nobody reads.

The classes are a closed set, checked by `make check-docs`; a new class is
added here and in `scripts/check_docs.py` in the same commit:

```
unpinned unshaped-input traceback-at-boundary suppression site-fix
caller-sourced partial-write name-drift empty-default
```

Status is `open`, `promoted → <mechanism>` or `expired <YYYY-MM-DD>`. Commits
are cited by short hash from `main`, which is why the developer merges with a
merge commit and never a squash (CLAUDE.md → Git workflow); a round is the
`/review-round` number.

| Class | Where | The mistake | The invariant restored | The pin | Status |
|---|---|---|---|---|---|
| `unpinned` | 0a `9105e57`, `3113c9d`, `48e088f`, `8e3f105`, `47a6abd`, `e5f650a`, `7a42e4d`; 1 `6794489`; 3a `9e36936`; 2 round 2 (`af3c898`, `e7c0df8`, `6208c54`, `04de832`); 6b `29e3a93`; 7b `3f6ef62`; 8a `d4275d7`; 8b rounds 1–3 (`717874a`, `bc9666c`, `337935d`); tooling round 3 (2026-09-07: the parse-error closed set carried a dead `ValueError` arm and an unpinned `RecursionError` arm); 9a round 2 (2026-09-07 `631aa09`: the study export's rating-trend order-by omitted `source` from the `(source, profile, month)` grain, so two same-month sources appended in engine order); 9b-fix round 1 (2026-09-08 `c21caed`: the theme marts grouped `share` by `run_id`, so correctness relied on `run_id` being single-valued and no test failed if a second value split the grain — `min()` carries it off the grain; `check_pins` cannot catch a grain-split on an already-pinned mart, so a second-run_id scenario test carries this case); 9b round 1 (2026-09-08 `013ebf6`: the corpus panels' chart kind was a literal on an already-pinned builder, so a line↔grouped_bar swap failed no test — over synthetic they render the fixture state, so the bytes never cover the real chart; `check_pins` cannot catch a kind swap on a pinned panel, so a per-kind render scenario test carries it); 9b round 2 (2026-09-08 `1b21215`: invariant 3's pin asserted `var(--sN)` over the whole page, which the fixture CSS satisfies unconditionally, so drawing the band in a categorical slot failed no test — a page-scoped assertion is a pin that pins nothing; `cee1d1a`: the legend was dropped for a single-series panel, so a band-only corpus hid the band's count and the invariant's own test could not tell — the scenario was unwritten); 9b exit round (2026-09-08 `eb79c13`: B2.5 drew every theme where SPEC and BACKING claim the held-claim share — the drawn label set was pinned to what the code did, not to the claim) | A rule, a key, a boundary, a pairing or a tiebreak was written and worked, and no test failed when it moved: the hold-rule boundaries, the claim-draw memo's cache key, the `make simulate` rule/value pairing, the staging `content_hash` tiebreak; a closed set's arm nothing exercised. The most frequent finding class since Phase 0a. | Every rule and key has one test that fails when it moves, written with the rule, not after the round. | `scripts/check_pins.py` (the gate's `pins` line, `make check-pins`); the "Pinning test" column of `/preflight` | promoted → `scripts/check_pins.py` (2026-09-07) |
| `unshaped-input` | 0a `f9cf6a0`; 2 `f2790a0`; 3a `1be2c0c`, `0ea4e12`, `81e4794`, `0f89f43`; 5a `46d6694`; 7b `b38d871`; tooling round 3 (2026-09-07: the read-boundary layout guard was a denylist regex of spawner names that `os.popen`/`os.system` slipped past; now an import allowlist); 9a round 2 (2026-09-07 `991215a`: the study export interpolated `tag.lower()` into a chip's `class` attribute, safe only on the trust that `check_panel` ran first — now a closed tag→class lookup); 9c round 1 (2026-09-09 `6b198b8`: `_range_mark` keyed the fixed-mark guard on the three cells being equal, so a malformed params row with `high == low` and the default off it slipped through into `(default - low) / (high - low)` — a ZeroDivisionError out of render, safe today only because `check_parameter` enforces the ordering upstream; the guard now keys on the zero-width span it must avoid) | A foreign value was accepted by a loose test — `str.isdigit`, an unanchored pattern, whatever `json.loads` calls a number — and coerced or passed through: `N`, a DAMIR amount, a rating, a Crawl-delay, a hook event. | A foreign input is parsed to one declared shape with bounds and refused by name; the shape is written whole (`\A…\Z`). | code-craft → Guards; secure-by-construction → What we do not own; Before reporting DONE #8 | promoted → code-craft → Guards (2026-09-05) |
| `traceback-at-boundary` | 0a `ba407ce`; 1 `0f7b76c`; 2 `abe5a0b`; 3a `1bcd7d0`, `34d9631`; tooling round 1 (2026-09-07, `af55dec`, `fc12c18`: a non-UTF-8 tracked file through check-pins and check-docs); tooling round 3 (2026-09-07: the same file through the suite's five repository scanners, `tests/test_ingest_layout.py`, `test_model_marts.py`, `test_llm.py`, `test_noqa_reasons.py`, `test_labels_isolation.py` — the gate's `test` line then carried tracebacks); 9a round 2 (2026-09-07 `9dd1b7c`: the study export coerced a mart cell — `float(rating)`, `None.startswith` — with no null guard, so a null value or provenance cell escaped `render` as an uncaught traceback, not a one-line refusal) | A refusal surfaced as a traceback: a bad `TARGET`, an existing capture directory, a missing executable, a regex that hung, a declaration the engine could not run — and, after promotion, a strict `read_text` with no catch. The recurrence showed the sentence covered process boundaries and not reads. | At a process boundary every refusal is one line naming the input and an exit code; exceptions stay inside the library. A read is a boundary too. | code-craft → Error policy (two sentences); `tests/test_review_tools.py::test_cli_refusals_are_one_line_exit_2` and its siblings; `tests/test_check_pins.py::test_a_blob_that_is_not_text_or_does_not_parse_is_a_refusal`; `tests/test_check_docs.py::test_a_file_that_is_not_text_is_one_error_line_in_every_check`; `tests/test_review_tools.py::test_run_reports_output_that_is_not_text` (the subprocess boundary); `::test_every_script_reads_and_runs_through_the_shared_boundary` (no raw read or run under `scripts/`); `tests/repo_text.py::repo_text`, the suite scanners' one reader, pinned by `tests/test_repo_text.py` | promoted → code-craft → Error policy (2026-09-05; reopened and re-closed 2026-09-07 with the read sentence, then re-implemented as one boundary in `review_common` after round 2 found the sentence applied at its sites only) |
| `suppression` | tooling `527425a`, `ff6d89a` (2026-09-05) | A function-shape signal was silenced with a `noqa` — three on `scrape` — instead of a split, and a `noqa` that suppressed nothing stayed. | Past a signal a function is split or carries a one-line reason; a suppression that suppresses nothing is deleted. | RUF100 (pyproject); `tests/test_noqa_reasons.py` | promoted → RUF100 + `tests/test_noqa_reasons.py` (2026-09-05) |
| `site-fix` | 2 round 4 `20240c8` (missed in round 3); 3a exit (BACKLOG row "Round 5's fix classes were applied at their finding sites only"); tooling round 2 (2026-09-07: round 1's read-boundary fix, `af55dec`/`fc12c18`, covered the named sites only — re-implemented in `f663d20`); 9b challenge round 2 (2026-09-08 `7912825`: round 2's fix of the page-scoped pin, `1b21215`, landed at F1's site while two sibling assertions in the same file stayed page-scoped — the `/selfcheck` grep step was not run between the fix and the challenge); 9c round 1→2 (2026-09-09: round 1's `_range_mark` zero-width guard `6b198b8` hardened the divide-by-zero branch but left the sibling geometry hazard in the same function — a `default` outside `[low, high]` still drew the dot past the range line; the exit round's code-reviewer found it and `b3757cf` clamped the fraction, completing the guard-by-shape over the whole range mark) | A fix landed on the finding's line while sibling sites kept the flaw: the driver-error relay around `create` but not `_columns`; one of six shapes anchored; the read guard on checks 7 and 8 while check 6 and the tokenizer's other exceptions stayed raw; a geometry guard on the zero-width case but not the out-of-range default in the same function. | A fix changes the mechanism's kind everywhere the class occurs — the class, not the case; the fix commit greps the class and lists the sites. | CLAUDE.md → Workflow rules "Fix the class, not the case"; code-craft → Guards, last bullet (the grep); `/selfcheck` (a3); for reads, `tests/test_review_tools.py::test_every_script_reads_and_runs_through_the_shared_boundary` | promoted → code-craft → Guards (2026-09-05; reopened 2026-09-07, re-closed the same day: the grep step in `/selfcheck` and, for reads, the layout test) |
| `caller-sourced` | 3a `b5ba7f3`, `057eac5`, `6d0627f`; 8b round 2 `28da07a`; 9a round 1 (2026-09-07: the study export decided "is this Pending?" from the `kind` proxy not the authoritative `tag`, and drew the footer's evidence tag from an authored literal not from the points it shows — the code-reviewer's Pass 2 caught both); 9b round 2 (2026-09-08 `505a5cf`: the Beat 2 readers wrote the point tag as the literal `"Measured"` while every Beat 1 reader projects the mart's `tag` column — the values-equal-mart test checked the value and not the tag; the code-reviewer's Pass 2 caught it again, the mechanism holding); 9b exit round (2026-09-08 `a3822f3`: the footer's brief §6 citation hung on "any URL present", a proxy, not on the Documented tag of the points shown) | A value or a decision the data determines was taken from a proxy field, a parameter or a default a caller could pass differently: the feed parser's platform as a module default, a rebuild's `root` rebound mid-loop, a claim draw handed in beside the fit it derives from, a panel's Pending state read off `kind`, a panel's shown tag authored rather than derived. | A value or decision the data determines is derived from the authoritative field where it is used, never a proxy, a caller's argument or a module default. | code-craft → Function shape (the sentence added 2026-09-07); code-reviewer Pass 2 | promoted → code-craft → Function shape (2026-09-07; the reviewer caught the 9a recurrence, the mechanism holding) |
| `partial-write` | 3a `0198fed`, `30d9fdd` | A loader wrote the rows before the refused one and stopped, leaving half a batch. | A load writes its batch or nothing: one transaction per load, and a refused row refuses the batch. | code-craft → Error policy (the sentence added 2026-09-07); the two-rebuilds tests per mart | promoted → code-craft → Error policy (2026-09-07) |
| `name-drift` | 6a `6310b66`; 8b round 2 `dfd299b`; fix/cost-outputs-unit round 1 `ebcc556` (2026-09-09: a day count's unit read `count`, the rounding bucket, once printed beside its value) | One quantity carried two or three names across the mart header, CLAUDE.md, the spec and the code (the hold-length threshold); test names drifted from the spec's Evidence ids; a quantity's unit named its format bucket, not its dimension. | One concept, one name, in code, mart headers, SPEC.md, BACKING.md and the spec; the mart's name is canonical. | `/preflight`, column "Same concept, other names"; code-craft → Naming (the sentence added 2026-09-07) | promoted → `/preflight` + code-craft → Naming (2026-09-07) |
| `empty-default` | tooling round 2 (2026-09-07, `f663d20`: a failed `git ls-tree` blanked the test pool into "every symbol unpinned"); tooling round 3 (2026-09-07: an unreadable `BACKLOG.md`/`DECISIONS.md` read as empty records, so every tag citing a real row was reported; an unreadable `Makefile` read as no targets, so every named `make X` was "not in the Makefile"); 9b exit round (2026-09-08 `92e29e6`: a panel-level source the drill could not shape fell to "source pending" — now an address, a repository file, or a refusal by name) | A reader that failed handed back an empty default — no tests, no records, no targets — and the check downstream reported the emptiness as findings, a fan-out of false lines beside (or instead of) the one naming the cause. | A reader that fails returns nothing the caller can check against: the failure travels with the value (`None`, a `Refused`, an `(empty, error)` pair) and the caller reports the one line and stops. | code-craft → Error policy (the sentence added 2026-09-07); `tests/test_check_pins.py::test_a_failed_test_listing_is_a_refusal_not_an_empty_pool`; `tests/test_check_docs.py::test_a_file_that_is_not_text_is_one_error_line_in_every_check` (the record and Makefile cases); `tests/test_review_tools.py::test_gate_prints_one_line_per_check_and_the_total` (the evidence case) | promoted → code-craft → Error policy (2026-09-07) |
