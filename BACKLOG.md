# BACKLOG.md — deferred findings and revisits

Items accepted "for now" with a concrete revisit trigger. Reviewed at every
phase exit (alongside the coherence audit); an item whose trigger has arrived
is either done in that phase or re-deferred here with a new trigger — never
silently dropped. Cite rows by TITLE (bold text); line numbers shift. A closed
row is struck through with "DONE Phase N" (or the fix branch), never deleted.
CLAUDE.md states the open-row count; `make check-docs` keeps it honest.

| Item | Source | Trigger |
|---|---|---|
| **No mutation sweep** — the reference project's `mutate.py` (delete-call, constant-return, invert-guard, swap-sort-key over Python; arm operators over SQL `case`) is not adopted. Load-bearing logic here (rules classifier, cost formulas) is pinned by goldens and the Phase 8 "code formulas equal displayed numbers" test; the functionality-tester hand-mutates in a worktree per review. | Phase 0a (`docs/PLAN.md` §2) | A bug in `classify/` or `models/` that a green suite missed, or a hand-mutation survivor class the tester reports twice: adopt the sweep for that class only |
| ~~**CI green on the Phase 0a PR is unverified until first push** — Done-when 6 / Evidence row 6; the workflow has never run (branch unpushed).~~ DONE Phase 0a — `ci / lint-test` green on PR #1's first run (2026-09-01). | Phase 0a spec | First push of `phase-0a-machinery`: confirm `ci / lint-test` green, then strike |
| ~~**Naming-the-target check is agent-only** — no insurer may be named as the target of the study in prose, code, comments or commits (PROJECT_BRIEF §2.5); today only `study-editor` and `code-reviewer` check it. A deterministic grep would put the name in the repo; a hashed denylist (sha256 of lowercased tokens over `*.py`, `*.sql`, `*.md`, commit messages; seed CSVs and URL columns excluded) is the mechanical alternative.~~ DONE tooling/review-stack (2026-09-05) — `make check-docs` check 6: sha256 of every token in tracked code, prose and workflow files and the last 50 commit messages, URLs stripped, against `scripts/neutrality_hashes.txt`; `ingest/sources.py`, `fixtures/` and `data/` excluded as recorded. | Phase 0a (`docs/PLAN.md` §6.10, default taken) | Re-deferred at the Phase 3a exit: the name reached `ingest/sources.py` under D1, the one file that may carry it, and DECISIONS records that the future hashed check excludes those declaration fields; build it when a second brand-carrying file appears, or at Phase 4's first weekly commit. Re-deferred at the Phase 4 exit: the weekly commit is a fixed brand-free template under the D1 `git ls-files` walk, so the name cannot leak through it — build the hashed check when a second brand-carrying file appears |
| **Link check skips titled and reference-style links and checks example links inside fences as real** — `[x](a.md "title")` and `[x][ref]` are never resolved; a link inside a code block is checked. Accepted: no doc uses those forms today. (The other two halves of this row closed in round 2: `slug()` emits one hyphen per space as GitHub does, and `anchors()` strips fences.) | Review round 1, Phase 0a (code-reviewer #17/#18) | The first false result seen (a missed broken link or a false "broken link" from a fence): extend `_LINK` and strip fences in `check_links` |
| **`check_backing` parses only the first table with the §8 header** — rows in a second table with the same header are never checked. Accepted: BACKING.md has one table by construction. | Review round 1, Phase 0a (code-reviewer #21) | A second table appears in BACKING.md: parse every matching table, or refuse a second header |
| ~~**"A Pending row has no number in SPEC" is not checked mechanically**~~ DONE Phase 9a — `study/export.py`'s render contract refuses a Pending panel carrying a value by construction (`check_panel`), pinned by `tests/test_export.py::test_a_pending_panel_with_a_value_is_refused_one_line_nonzero`; `make study` in CI renders and diffs the committed bytes. | Review round 1, Phase 0a (coherence-auditor) | Phase 9 export: the render-time refusal lands with a test, then strike; or earlier if a SPEC.md Pending panel is caught carrying a number |
| **`check_citations` strips fenced blocks but not inline code spans** — a real-digit `B<beat>.<n>` inside an inline `` `code` `` span in SPEC.md would count as a citation; today SPEC.md's inline-code B-ids are `B<beat>.<n>` placeholders the regex does not match, so it does not bite. | Review round 1, Phase 0b (code-reviewer) | A real-digit B-id appears inside an inline code span in SPEC.md: strip inline spans before extracting citations, or refuse the span |
| ~~**`reviews_per_month` is a query in `pipeline/metrics.py`, not a mart** — the Phase 2 metric is pipeline health, not a study claim, so a `sql/marts/` file for it would be an orphan under BACKING.md's rule.~~ DONE Phase 9e (2026-09-10) — the Phase 9e challenge (#6) settled that folding it into `pipeline_row_counts` would make that mart two-grain, and that a query is not an orphan (the rule bites marts only); the query relocated to `pipeline/build.py` beside `table_counts` (still printed by `make rebuild`, still portability-linted at its new home), `pipeline/metrics.py` deleted, no mart made. | Phase 2 spec (pinned decision 5) | B5.2 `pipeline_row_counts` lands (Beat 5): fold the query into that mart, delete `pipeline/metrics.py`, strike |
| ~~**Label arity is undecided** — the brief (§5) lets a review carry several themes; CLAUDE.md and PLAN §4.3 sketch one `theme` column per row. Single vs multi-label changes what a "theme share" chart means.~~ DONE Phase 0b — the grain is one row per review × theme, stated in SPEC.md's Beat 2 and CLAUDE.md's Classification contract; the code invariant landed in Phase 5b (`tests/test_rules.py::test_review_times_theme_grain` — a review matching K theme groups is K rows). | Review round 1, Phase 0a (coherence-auditor) | Phase 0b spec: decide (one row per review×theme is the likely shape), state it in SPEC.md's chart definitions and CLAUDE.md's Classification contract before any chart is frozen |
| ~~**Health details arrive in review bodies** — reviewer-written `title`/`body` text can name a condition (the first, since-deleted live capture had a few such bodies). The parser never reads names and nothing under `data/` is published, so nothing is exposed today; the rule for excerpts is not yet written.~~ DONE Phase 9b — the export reads only an allowlisted column set (`study/panels.py::ALLOWED_COLUMNS`, checked on each cursor's description so `select *` fails by name), so no review text (`title`/`body`) can reach the page; pinned by `tests/test_beat2.py::test_every_export_query_projects_only_allowlisted_columns`. The review-level drill is deferred to the Metabase demonstration (9g). | Review round 1, Phase 2 (security-reviewer #6) | The first tracked file or published panel carrying review TEXT (Phase 4's capture commit if it ever carries bodies, or Phase 9's drill-through excerpts): paraphrase or minimal excerpt per brief §2.5, decided before it lands. Re-deferred in Phase 3a: `data/snapshots/manual_snapshots.csv` is the first tracked file under `data/snapshots/`, but it carries numbers only. Re-deferred at the Phase 4 exit: the weekly cron's tracked `fetched_snapshots.csv` carries numbers only (no review body), so the first tracked review TEXT is still Phase 9's drill-through excerpts. Re-scoped in Phase 9a: the excerpt/paraphrase rule is authored as a DECISIONS contract (the one render path for review text), but Beat 1 ships no review text (B1.1 is Pending), so the enforcing test and the first text surface land in Phase 9b (Beat 2's drill-through-to-excerpts) — this row stays open until then |
| ~~**`FIXTURE=cache` names a mechanism and calls real rows a fixture** — study-editor proposed `ROWS=captured\|none\|synthetic\|frozen-sample`; it touches the DONE command, the Makefile, CI and every doc, so it was deferred rather than folded into a review fix.~~ DONE Phase 3a — `ROWS=captured\|none\|synthetic\|samples` (D4). | Review round 1, Phase 2 (study-editor #5) | Phase 3a's spec, which adds the next rebuild input: rename there in one commit, or record why the name stays |
| **`read_captures` parses every capture into memory before any is loaded** — one hand-corrupted page blocks the loading of every other capture, and a growing weekly cache is fully materialised per rebuild. Accepted: today's cache is small and a corrupt page is a one-line refusal naming its capture. | Review round 2, Phase 2 (code-reviewer #10) | Phase 4's weekly cache passes ten captures, or the first time one corrupt capture blocks a rebuild the developer needs: make `read_captures` a per-capture generator so the capture stays the unit. Re-deferred at the Phase 4 exit: on the CI runner each weekly run holds one fresh capture per source; the local cache is still small |
| ~~**Robots patterns compile to `.*`-joined regexes with no wildcard bound** — a pathological robots file could make our own matcher backtrack: measured in round 4 at about ten times slower per added `*` on a 60-character path (seven stars, 7.7 s), so ten stars would hang `make scrape`. Accepted: one host today, its file is plain, and the declared source is not fetched.~~ DONE Phase 3a — `ingest/robots.py::_matches` is a direct two-pointer match; thirty wildcards answer in milliseconds (pinned). | Review round 2, Phase 2 (code-reviewer #8) | Phase 3a adds a second host: bound the wildcards per pattern or match linearly, with a pathological-file test |
| ~~**`DEFAULT_CACHE` is bound in two modules, and meta validation ties a rebuild to the fetch-time `ALLOWED_HOSTS`** — the hint and the check could name different roots, and shrinking the host list would make legitimately captured rows unloadable. Accepted: one root, one host, both pinned.~~ DONE Phase 3a — `ingest/sources.py::CACHE_ROOT` is the one binding; a capture's meta is checked against its source's declared host. | Review round 2, Phase 2 (code-reviewer #11) | The first change to `ALLOWED_HOSTS` or a second cache root (Phase 3a): one binding for the cache root; meta validation against the hosts that were allowed when the capture was written |
| ~~**The capture path is hardwired to one platform** — `pipeline/build.py` imports `read_captures` from `ingest.app_store` only, fixes the cache root at `data/cache/app-store` and the `run_id` prefix `app-store:`; `ingest/sources.py` fixes one `FEED_HOST` for every declared source; `ingest/fetch.py` calls the App Store parser for whatever source it is given. Accepted: one platform in Phase 2 by design; `make scrape` has no green path today (the one source is recorded as not to fetch), so the corrected robots gate first runs live in 3a.~~ DONE Phase 3a — every source declares its parser, host, pages and cache directory; nothing branches on a name. | Exit audit, Phase 2 (coherence-auditor #9) | Phase 3a's first source on another platform: a parser and a cache root declared beside each source in `ingest/sources.py`, never a branch on its name |
| ~~**`fixtures/app-store/robots.txt` is permissive, frozen and read by nothing** — only the MANIFEST test hashes it; the real file for that source disallows the feed; a rebuild never consults it.~~ DONE Phase 3a — re-frozen to the real rule (`Disallow: /*/rss/*`) and read through the matcher by `tests/test_robots.py`. | Exit audit, Phase 2 (coherence-auditor #10) | Phase 3a's spec: a test reads the frozen file through `ingest/robots.py`, or it is re-frozen to the real rule with a DECISIONS entry and a `Freeze:` line — decided there, or record why it stays |
| **The App Store listing's machine-readable block is unverified** — Apple's website terms forbid robots on the listing, so its rating and count are read by hand (`data/snapshots/manual_snapshots.csv`); whether the page carries a JSON-LD `aggregateRating` like the Google Play listing was never checked from the repo. | Phase 3a (candidate checks, 2026-09-02) | Any future permission to fetch that listing: verify the block and declare the source with `parser=listing`; otherwise the hand-read path stands |
| **A re-fetch reads every page of a review profile** — `make scrape` fetches a source's declared pages in full each run; a weekly run re-reads pages whose reviews are all already known. Accepted: one profile of fourteen pages, ≥ 2 s apart, once a week. | Phase 3a (pinned decision 4) | Phase 4's weekly workflow: stop a review-page source at the first page whose every review is already in raw, with a test on two captures. Re-deferred at the Phase 4 exit: the weekly run re-reads all declared pages politely (≥ 2 s/host, ≤ 60 pages) — stop-at-known-page not built |
| **An Opinion Assurances review's id is a content hash** — the page marks no stable review identifier, so `external_id` = sha256(publication date, experience date, rating, body). A re-capture of an unchanged review adds nothing, but an edited review becomes a new review rather than a new version of one, so distinct-review counts can overstate by the number of edits. | Phase 3a (amendment A1; STOP-and-decide, 2026-09-02) | Phase 4's first two weekly captures: count reviews whose body changed between captures (same dates and rating, different hash); if edits are more than a handful, key on (publication date, experience date, rating) and treat the body as content, with a DECISIONS entry. Re-deferred at the Phase 4 exit: no two real weekly captures exist yet — revisit after the cron's first fortnight |
| **The Opinion Assurances stat figures are layout text** — the one-star share, response rate and response delay the profile page displays sit in progress bars and labels, not in microdata, so the fetched snapshot carries rating and count only; B1.4's Opinion Assurances point stays Documented from the anchor while the platform is fetched weekly. | Phase 3a (structure dump, 2026-09-02) | The page marks those figures as data (a later dump shows an `itemprop` or `content`), or Phase 4 decides that a hand-read row may complement a fetchable source's capture for figures its parser cannot read (today refused: a fetched source's figures come from its capture) |
| **The D1 walk covers tracked files, not commit messages** — `tests/test_ingest_layout.py::test_brand_carrying_strings_appear_only_in_the_declarations` walks every tracked file for the declared brand tokens; commit messages, which D1 also names, are covered by review only (the branch's log is clean as of round 2). | Phase 3a (round 2, security-reviewer #4) | The first fix commit that quotes a source declaration, or the Phase 4 weekly commit: extend the same walk over `git log <base>..HEAD` subjects and bodies in `make review-gate`. Re-deferred at the Phase 4 exit: the weekly commit's message is a fixed brand-free template literal in the tracked `weekly.yml`, already scanned by this walk and pinned by `tests/test_weekly.py`; the general git-log walk over arbitrary commits stays deferred |
| **The Snowflake half of `default_schema` is unpinnable offline** — `warehouse.default_schema` reads `current_schema()` from the engine; a `"main"` literal in its place survives the suite, since DuckDB's answer is `main` (a round 5 hand mutation). | Review round 5, Phase 3a (functionality-tester #8) | Phase 10's Snowflake demonstration: run `idempotency-check` there and pin the schema name that engine returns |
| **`make review-gate` without `SPEC=` is red on a phase branch, and its two summary lines count different things** — the no-SPEC form runs the fixtures check with no `Freeze:` line to read, so a re-frozen fixture fails it, and the failure line counts checks failed where the OK line counts checks passed. | Review rounds 3–5, Phase 3a (functionality-tester #9) | The next change to `scripts/review_gate.py`, or a `fix/` PR: the no-SPEC form reads the branch's spec or skips the freeze check, and both lines count the same thing. Phase 3b's gate ran green — A1 added no fixture, so the freeze check did not bite. The trigger arrived at Phase 9i's exit (2026-09-12): its first freeze of `fixtures/ameli/` is the same mechanism as a re-freeze and the no-SPEC form is red on the branch; re-deferred to its own `fix/` PR, never a phase diff |
| ~~**The confirm gate does not hold against an environment that chooses what make reads or runs** — `MAKEFILES=evil.mk` with a `$(shell echo $$PPID > data/.confirm)` line, or a `PATH` that replaces `uv`, arms the gate with no `confirm` goal (exit pass: exit 0, corpus deleted).~~ DONE Phase 4 — decided: the weekly workflow's environment is trusted as the runner's own (it runs on `schedule`/`workflow_dispatch`, never on untrusted PR input), so `MAKEFILES`/`PATH` are the repo's; the residual stands only outside that trust boundary (a developer's local machine), as the Threat model and DECISIONS state. | Exit pass, Phase 3a (security-reviewer #2) | Phase 4's workflow (which runs `make confirm scrape` unattended in CI): decide whether a set `MAKEFILES` is a refusal there, or whether the workflow's environment is trusted as the runner's own |
| **The make-level gate probes write the working tree's stamp** — `tests/test_makefile.py` runs the real make in the repo root, so `make test` creates, plants and consumes `data/.confirm` beside the developer's corpus and redefines `reset` as a probe (the override is now asserted: the corpus file survives it); a suite killed past its `finally` leaves `99999` there. | Exit pass, Phase 3a (security-reviewer #3, code-reviewer #2) | The next change to `tests/test_makefile.py`, or the first wedged `confirm` after a suite run: point the stamp at a directory the test owns (a worktree or a resolved root) and assert the corpus file survives the probe |
| ~~**Round 5's fix classes were applied at their finding sites only** — the driver-error relay wraps the scratch `create` but not `_columns` or the `drop`; `ingest/opinion_assurances.py` still matches its itemtype and sentence patterns with an anchored `match`; the shape patterns are unanchored, so the whole-value property lives at each call site; the anchors reader accepts `profile = sample` (the loader refuses its labels, so nothing loads).~~ DONE Phase 3a (exit) — the relay covers the whole scratch block, every shape is `\A…\Z` and the profile parser matches whole, the anchors reader refuses the sample profile. | Exit pass, Phase 3a (code-reviewer #3 #6 #7 #8) | The next change to any of those modules, or the next parser: apply each class at every site of its kind in that module, with `\A…\Z` in the patterns. (Phase 3b built no parser — Trustpilot is hand-read.) |
| ~~**The §6 response figures are not seeded**~~ DONE Phase 9a — the resolution taken was the stated-waits one: B1.4 renders a note beside the chart that the answer-rate and time comparison waits for a second platform's figures (`tests/test_export.py::test_b1_4_states_the_response_comparison_waits`), no fixture re-freeze. | Exit pass, Phase 3a (coherence-auditor #2) | Phase 9's B1.4 panel: place them by A4 (e)'s midpoint rule with a `Freeze:` line, or state beside the chart that the comparison waits for a second platform. (Phase 3b did not capture Trustpilot — not fetchable — and its response figures are layout, not read; SPEC B1.4 already states the comparison waits.) |
| ~~**Phase 4 has no tracked path into `platform_snapshots`** — captures are read only from the gitignored `data/cache/`, and the one tracked input (`manual_snapshots.csv`) refuses a source that has a parser, so the weekly commit under `data/snapshots/` needs a tracked capture root or a third `origin` this phase did not build.~~ DONE Phase 4 — the numbers-only tracked file `data/snapshots/fetched_snapshots.csv` (origin `fetch`) is the fetched series' home: `make record-snapshots` harvests it from the capture, `rebuild ROWS=captured` reads it, and the double read with the live cache is a no-op. | Exit pass, Phase 3a (coherence-auditor #10) | Phase 4's spec: decide the tracked shape (a capture root under `data/snapshots/` read like the cache, or an aggregates file with its own origin) before the workflow is written |
| ~~**A fetched peer may not join its anchor's series** — peer anchors carry profiles like `peer-traditional-1`; a Trustpilot peer declared in 3b chooses its own `profile`, and `rating_trend` / `peer_ratings` key on `(source, profile)`, so one peer can become two series.~~ DONE Phase 3b — the Trustpilot hand-read source declares `profile = fr-digital-first`, exactly the Trustpilot anchors' profile, pinned by `test_snapshots.py::test_trustpilot_hand_read_row_matches_its_anchor_seed`; the same rule binds any future Trustpilot point. | Exit pass, Phase 3a (coherence-auditor #11) | Phase 3b's spec: a fetched peer's declaration reuses its anchor's profile name, pinned by a test that the seed and the declaration agree |
| ~~**The Trustpilot review-profile parser and a frozen sample are not built** — Trustpilot's robots.txt disallows our crawler on every path (`User-agent: *` → `Disallow: /`), so no page may be fetched or frozen; the profile's figures are hand-read (Measured) like the App Store listing's, and only the rating and count are read, not the per-review rows or the response figures.~~ DONE Phase 3c — written authorization arrived, so the reviews are imported as an authorized OFFLINE export (not a live fetch): the `trustpilot` parser reads the export capture into per-review rows, a synthetic sample runs under `ROWS=samples`, and `fr-digital-first-trustpilot-reviews` is declared (`parser=trustpilot`, `fetchable=False`, the crawler never runs). | Phase 3b (amendment A1, 2026-09-03) | A future written authorization from Trustpilot (as Opinion Assurances granted): freeze a sample from one permitted page, then build the parser |
| **A live Trustpilot fetch and a scheduled refresh are not built** — Phase 3c imported the reviews once, from an authorized offline export (`fetchable=False`, the crawler never runs against Trustpilot); the response-rate/delay §6 figures are not in the export, a live fetch would need `fetchable=True` and still faces the robots ban, and no periodic refresh of the export exists. | Phase 3c (2026-09-04) | A need to refresh the corpus, or authorization to fetch live: a new phase declaring the fetch path or the refresh cadence, its own capture per run |
| ~~**CLAUDE.md is over the ~400 cap** — 443 at the Phase 2 exit, +88 in 3a, ~518 now, mostly the `rebuild` and `confirm` Commands entries and the status block. (The Phase 3b/3c exits reported it but did not trim: the reduction is its own cleanup, not phase work — re-deferred. Re-deferred again at the Phase 5a exit: ~550 lines, under the 600 alternate trigger, and 5a is not the last Phase 5 sub-phase. Re-deferred again at the Phase 5b exit: 565 lines, still under the 600 trigger; the trim touches the Commands, Repo map and status blocks and is its own cleanup diff, not to be mixed into the rules-layer PR — one phase, one diff. Re-deferred at the Phase 9g exit: 805 lines, past the 600 trigger, which the Phase 6–9f exits did not act on; the trim is the next `tooling/` PR, before the Phase 10 spec is finalized.)~~ DONE on `phase-9h-sample-mean` (2026-09-11, the trim's own docs-only round first): 805 → 592 lines (597 after Phase 9h's status paragraph) with no rule cut — the Repo map, Commands, Working with the model, Project tooling and Current status now hold only what the specs' Delivered paragraphs, DECISIONS, `make help` and each hook's header do not; the rules sections alone are ~330 lines, so the cap is restated as 600 (DECISIONS → Process). | Exit pass, Phase 3a (coherence-auditor #8; the Phase 0a decision asks the audit to report growth); re-deferred at the Phase 3c, 5a, 5b and 9g exits (coherence-auditor) | Before the Phase 10 spec is finalized (the 600-line trigger fired at 805): a dedicated `tooling/` CLAUDE.md cleanup PR that moves the Commands' mechanism sentences into the spec's Threat model and `make help`, and the status block's amendment history into DECISIONS |
| **Two scheduled runs are not yet observed in Actions** — the brief's Phase 4 done-when is "two scheduled runs visible in the Actions history"; that is observed over weeks, so the merge proves the path offline (the DONE command) and the live cron proves itself later. | Phase 4 spec (done-when is a post-merge ops check) | After the second Monday post-merge: confirm two `weekly` runs in the Actions history and that `data/snapshots/fetched_snapshots.csv` gained rows; if not, debug the schedule (a workflow on a quiet repo is disabled after 60 days — a manual dispatch keeps it live) |
| **A weekly scrape refusal aborts the whole commit** — `make confirm scrape` exits 2 if any source refuses (a transient robots status, a page shape), so a week where one fetchable source fails commits nothing, even a source that succeeded. Accepted: a failed weekly run is visible and retried next week; no partial or fake row is ever written. | Phase 4 (2026-09-03) | The first week one source refuses while another succeeds: decide whether to record the successful source's figures anyway (a per-source harvest that tolerates a partial scrape) |
| **The real 300–500 eval labels are not yet in the answer key** — Phase 5b committed the *synthetic* corpus's 39 ground-truth rows to `classify/eval/labels.csv` and grades the rules on those (fabricated reviews, no personal data); the real corpus's hand labels are still human offline work (draw a sheet with `make label-sample N=`, decide each review's themes from the closed set, append `(review_id, theme)` rows). Until they exist, precision on REAL rows has nothing to score. | Phase 5a (2026-09-04); re-scoped Phase 5b (2026-09-04) | Phase 7 runs the rules on real scraped rows: the developer labels a real sample offline first; the per-theme precision test then scores real rows against those labels (the synthetic ground truth stays for CI) |
| ~~**The labels-isolation grep covers four surfaces, by substring** — `tests/test_labels_isolation.py` scans `classify/` (minus `eval/`), `pipeline/`, `sql/` and `models/` for the reader tokens (`labels.csv`, `labels_io`, `read_labels`, `LABELS_CSV`); `ingest/`, `dags/`, `study/` and `scripts/` are not scanned, and a dynamically built path (`"labels"+".csv"`) would evade a substring match.~~ DONE Phase 6a — the swept set now covers every code surface (`ingest`, `dags`, `study`, `scripts` added; the non-existent dirs are skipped until they land), and `classify/llm.py` is inside the swept `classify/` set, pinned covered (`test_the_model_call_site_is_covered_by_the_wall`). The import-graph assertion stays unbuilt: no dynamic-path evasion (`"labels"+".csv"`) is real today — a fresh row if one appears. | Round 1, Phase 5a (code-reviewer, functionality-tester); closed Phase 6a | Phase 5b added `classify/rules.py` — already inside the swept `classify/` surface and pinned covered (`test_labels_isolation.py::test_the_rules_layer_is_covered_by_the_wall`); Phase 6a added `classify/llm.py` and widened the grep to every code surface |
| **The with-key classifier_quality figures are not pinned, and the synthetic held-out fold is only 5 clear reviews** — Phase 6b pins the *rules-only* (no-key) fold-4 grades in `tests/pins.py`; with a key the model decides the ambiguous reviews and the figures change (the model varies), so a with-key run is developer-run and its numbers are unpinned. And the five synthetic held-out reviews are clear cases the rules classify correctly, so every scored label is 1.0/1.0 or `None` — the metric's discriminating power (a disagreement scores below 1) is proven only by a crafted unit test (`test_gate.py`), not by the fixture. | Phase 6b (2026-09-04) | Phase 7 grades the real corpus (with a key, against the real 300–500 labels): a real held-out fold will carry disagreements, so precision/recall land off 1.0; decide how to sanity-check the with-key numbers without pinning a nondeterministic value |
| **No `make classify-gate` diagnostic** — the held-out precision/recall report is visible only via `make rebuild` (it lands in the `classifier_quality` mart and prints a one-line summary), unlike 5b's `make classify-eval` tuning-fold report. A developer wanting the held-out report without a full rebuild has no target. | Phase 6b (2026-09-04, out of scope) | A developer needs the held-out report standalone: add a read-only, offline `make classify-gate` mirroring `classify-eval`, reading the built corpus's predictions |
| **The classify-step mart writes are not warehouse-aware** — `pipeline/build.py::classify_step` (moved there from `_classify_and_print` in `fix/idempotency-classify`) opens `connect("duckdb", …)` for the `classifier_quality` (6b), the theme-share `stg_classified_reviews`/`theme_share_*` writes (7a) AND `review_drill` (9g, via `build_post_classify_marts`) regardless of `TARGET`, so under `TARGET=snowflake` they would land in the local DuckDB file while the rest of the build targets Snowflake. Snowflake is deferred to Phase 10, so it is not a defect today. | Phase 6b (2026-09-04, round 1 functionality-tester); widened Phase 7a; `review_drill` added to the path Phase 9g (exit audit); code moved to `build.classify_step` in `fix/idempotency-classify` (2026-09-12) | Phase 10 wires Snowflake: the classify step's connections (reviews-per-month, cache read/write, the mart writes) take `TARGET` like the rest of `rebuild`; a test pins the marts land in the target warehouse; and `_do_idempotency` lifts its temporary `('duckdb',)` restriction (fix/idempotency-classify) so `idempotency-check` covers the classify path on the target engine too |
| **The "vs traditional" half of B2.2/B2.5 is empty** — every declared source in `ingest/sources.py` is `digital-first`, so `theme_share_by_month`/`theme_share_by_segment` split by one segment only; the study's hypothesis (held-claim complaints digital-first vs traditional mutuelles) has no traditional column yet. The marts are built and honest — they show the segments the data has. | Phase 7a (2026-09-04) | A traditional-mutuelle source is declared and captured: the marts gain a `traditional` segment. B2.5 then renders it with no code change (since the 9b exit round `_theme_series` labels its points by segment: x = segment, theme = series). B2.2 needs one: `_Q_THEME_BY_MONTH` filters `segment = 'digital-first'` and `_theme_series(…, "period")` has no segment dimension, so the by-segment split SPEC B2.2 promises is a second series set or a second panel — the phase that declares the source decides. Until then SPEC's B2.2 and B2.5 panels state the comparison waits |
| ~~**`make idempotency-check` skips the classify step, so the theme marts are not machine-checked by the target** — `idempotency_check` (`pipeline/build.py`) calls `rebuild()` only, which stops before classify, so `theme_share_by_month`/`theme_share_by_segment` (and `stg_classified_reviews`), and since Phase 9e `pipeline_row_counts` (B5.2, filled in the classify path), never appear in its per-table row-count diff; CI's `make idempotency-check` covers every other mart.~~ DONE `fix/idempotency-classify` (2026-09-12) — the classify step's code moved into `pipeline/build.py::classify_step` (the Repo map's home for the step) and `idempotency_check` now runs it after `rebuild()` with an isolated cache, comparing whole-db `table_counts`, so all six classify-path tables (`stg_classified_reviews`, the three `POST_CLASSIFY_MARTS`, `pipeline_row_counts`, and `classifier_quality` when graded) are in the diff; pinned by `tests/test_ingest_rebuild.py::test_idempotency_check_covers_the_classify_path_tables` (`pins.CLASSIFY_PATH_TABLES`). The two slow scenario tests stay for the stronger byte-identity check. | Review round 1, `fix/theme-marts-run-id` (2026-09-08, functionality-tester); trigger re-pointed at Phase 9b's challenge round 2 (#5); Phase 9e adds `pipeline_row_counts` to the same class (challenge #1); trigger re-pointed at the Phase 9f build (2026-09-10: 9f kept the committed page synthetic, so its `make study` never runs the classify step); closed `fix/idempotency-classify` | DONE — the classify step runs inside `idempotency_check`, whole-db `table_counts` compared, no carve-out for `classifier_quality` (a denylist-of-one), DECISIONS → Fix (2026-09-12) |
| **The DAMIR fetch's resource resolution and the full-month fit are unexercised in CI** — `opendata/fetch.py` resolves a month by its `A<YYYYMM>` title in the data.gouv resource list, then downloads it; this network path (and the fit over a real multi-GB month via `make sample-damir`) is developer-run only, so CI proves the fetch's gate and month-validation refusals but never the live resolution or a real download. A data.gouv title/format change would surface only when the developer next fetches. | Phase 7b (2026-09-04) | The developer's first `make confirm fetch-damir` after a data.gouv change fails to resolve or parse: adjust the title match / delimiter in `opendata/sources.py`, record it under DECISIONS → Gotchas |
| **A periodic Open DAMIR refresh is not scheduled** — the weekly cron (`weekly.yml`) captures review snapshots only; the DAMIR slice and its fit are drawn once, by hand, from a chosen month. A newer month would need a manual `make confirm fetch-damir` + `make sample-damir` + a fixture re-freeze (a DECISIONS entry). | Phase 7b (2026-09-04) | The study wants the claim-cost fit refreshed on a cadence, or a reviewer asks how current the DAMIR month is: decide whether to add a scheduled refresh or state the fixture's month as a fixed, cited vintage; the same question covers the data.ameli fee split (`fixtures/ameli/`, one year, Phase 9i): a newer year is a new hand download, `make slice-ameli YEAR=`, and a re-freeze |
| ~~**data.ameli practitioner-fee distributions are not ingested** — the brief (§7) names data.ameli practitioner-level fees as a second open-data source alongside Open DAMIR; 7b fits only the DAMIR reimbursed-amount distribution, which is what Phase 8's claim-cost draw needs.~~ DONE Phase 9i (2026-09-12) — the trigger reinterpreted: the `honoraires` table is annual per-practitioner totals, not claim amounts (nothing to fit); what it uniquely supplies, the extra-billing share, is one sourced B3.3 row (`extra_billing_share`) with its profession-family spread, from a hand-downloaded export (the host's robots file disallows a fetch). | Phase 7b (2026-09-04) | Re-checked at the Phase 8a and 8b exits: the cost model and the simulator both draw from the DAMIR amount fit alone, so re-deferred to the study. The study needs a practitioner-fee distribution the DAMIR amount cannot supply: declare the data.ameli source under `opendata/` and fit it the same closed-form way |
| ~~**Commands and skills sit in two layouts** — `/challenge` is a skill (it needs `allowed-tools` and `effort`); `/review-round`, `/phase-start` and `/selfcheck` are commands and keep working with the smaller frontmatter the skills reference documents. One layout would be tidier; migrating is cosmetic.~~ DONE tooling/review-stack (2026-09-05) — the three commands moved to `.claude/skills/<name>/SKILL.md` with `disable-model-invocation: true`; `tests/test_claude_config.py` widened. | Tooling (2026-09-05) | A fourth command is written, or a command needs a skill-only field (`effort`, `context: fork`, `allowed-tools`): move all of them to `.claude/skills/<name>/SKILL.md` in one commit and widen `tests/test_claude_config.py` |
| **`opendata/slice.py` reads a gzip member with no decompressed-byte cap** — a developer-run path on a known portal (the month is ~5.7 GB raw and is read as a stream), so no bomb reaches it today; the secure-by-construction standard asks for the cap or the written reason, and the reason is not in the file. | Tooling (2026-09-05, security standard) | A non-portal input reaches `open_amounts` (a fixture or a file a person supplies), or the fetch stops being developer-run: add a decompressed-byte ceiling with a one-line refusal, pinned by a test |
| **`challenge-gate.py`'s `ExitPlanMode` matcher is unverified against the installed build** — the hooks reference documents no payload for that tool, and whether a PreToolUse hook with `"matcher": "ExitPlanMode"` fires at all on this build has not been observed. The hook asks on every plan whatever the payload (a missing `plan` string is named in the reason), so a wrong field name costs nothing; a matcher that never fires means no `ask`, silently. Reopened at round 2 of the lean pass (2026-09-05): the hooks reference confirms the matcher is the tool name, so the wiring is right on paper, but the prompt has never been observed here. | Tooling (2026-09-05) | The first plan-mode exit in this repo: confirm the `ask` prompt appears; if it does not, read the event once (a debug hook printing `tool_name`) and pin the real name in the local wiring and the hook's test |
| ~~**A `Challenged:` stamp goes stale after an amendment** — the stamp is not tied to the spec's content, so an amended spec keeps its "challenged" state and the hook never reminds again; the round table it came from may no longer describe the plan.~~ DONE tooling/review-stack (2026-09-05) — the stamp carries `spec <8 hex>`, the hash of the Invariants and Done-when sections; the hook reminds with both hashes when they differ; `--spec-hash` prints it. | Tooling (2026-09-05, round 1 code-reviewer #5) | An amendment lands on a stamped spec without a new `/challenge` round and its review round finds a design fault the challenger would have raised: key the stamp to a hash of the spec's Invariants, or restamp per amendment |
| **`cost_per_contact` is a declared guess, not a sourced benchmark** — Phase 8a's cost model has no citable public customer-contact cost, so the parameter is `unsourced` on a slider; brief §7 allows either. | Phase 8a (2026-09-06) | A citable public customer-contact cost benchmark is found: flip `cost_per_contact` to `sourced` with its citation in `models/cost_model.py` (a one-cell change, no design change) Re-deferred at the Phase 9c build (2026-09-09): the page states it beside the parameter it concerns (B3.3's and B3.4's notes, B3.1's mean-claim row); the trigger stands |
| **The four scale anchors cite `PROJECT_BRIEF.md §6`, second-hand** — the cost model's `arr_eur`, `members`, `fraud_pool_eur` and `refunded_eur` cite the brief's §6, which records them as public disclosures with no address, each a floor; the study says so beside them. | Phase 8a (2026-09-06) | The developer hands over a disclosure address: add it as a brand-free declaration in `ingest/sources.py` under D1 (the one file that may carry a brand token), point the citation cell there, and extend DECISIONS + a security-reviewer surface when it fires Re-deferred at the Phase 9c build (2026-09-09): the page states it beside the parameter it concerns (B3.3's and B3.4's notes, B3.1's mean-claim row); the trigger stands |
| ~~**The claim-cost mean is the lognormal mean, not the fixture's arithmetic mean** — `mean_claim = exp(mu + sigma²/2)` over the fit; the fixture's own arithmetic mean is not a sibling the study can slide to, because it needs a new artifact row 7b's `write_fit` does not write.~~ DONE Phase 9h (2026-09-11) — the trigger reinterpreted (the page carries no slider; the reason is the reader's own recompute): `emp_mean` is the artifact's last row, a fourth fit parameter (a fixed mark) and the formula `claims_at_mean_cell = refunded_eur / emp_mean`, shown beside the claim volume in B3.3; `mean_claim` and `claims` unchanged. | Phase 8a (2026-09-06) | Phase 9 wants a `claims` slider spanning the sample mean: add one `mean` row to `opendata/fit.py::write_fit`, re-run `fit-damir` (a tracked-artifact change with a `Freeze:`-style DECISIONS line), and add it as a third fit sibling in `models/cost_model.py` Re-deferred at the Phase 9c build (2026-09-09): the page states it beside the parameter it concerns (B3.3's and B3.4's notes, B3.1's mean-claim row); the trigger stands |
| ~~**B3.3's headline derived figures live in a different mart from its BACKING cell** — B3.3's mart cell is `cost_model_params` (the raw sourced inputs `arr_eur`, `members`, `mu`, `sigma`, …), but its panel prose names two *derived* figures (revenue per member, claim volume) that are `cost_model_outputs` rows; the split is defensible (B3.3 = the sourced rows) and nothing is broken while no panel exists.~~ DONE Phase 9c — the first offered resolution: B3.3's headline reads the derived figures from `cost_model_outputs` (`study/panels.py::_headline_rows` filters `_formula_rows`, `study/text.py::HEADLINE_FORMULAS`); the parameter rows stay `cost_model_params`. | Phase 8a exit (2026-09-06, coherence-auditor) | Phase 9 wires the B3.3 panel: read the derived headline figures from `cost_model_outputs` (not `cost_model_params`), or re-point B3.3's cell — decided when the renderer lands |
| **The edit-loop hook defers invariant-guard tests** — the run-tests fast set (`-m "not slow"`) marks whole modules slow, so guards that live in them run only in `make test`/gate/CI, never in the edit loop: the no-key-green contract (`test_no_key`), provenance (`test_provenance`), the Makefile confirm gate / threat model (`test_makefile`), CLI variable validation (`test_cli`), tracked-output numbers-only (`test_snapshots`). A regression in one shows green in the edit loop. Accepted: the hook is a visibility aid by contract and the gate/CI run the suite plain. | Review round 1, tooling/run-tests-fast-set (security-reviewer, functionality-tester) | A guard regression that was green in the edit loop and cost time: split those modules' fast assertions into an unmarked module, or add a lightweight fast guard subset |
| **run-tests hook: an empty fast set would report silent green** — if `-m "not slow"` ever deselects to zero tests, pytest exits 5 ("no tests collected") which the hook maps to skip (exit 0), silently disabling the edit-loop check. Not reachable today (516 fast tests; `--strict-markers` now blocks the typo path). | Review round 1, tooling/run-tests-fast-set (security-reviewer) | The fast set can plausibly reach zero (a future mark-everything state): pin "fast set non-empty", or distinguish "empty repo" from "all deselected" in the exit-5 branch |
| **A person's turnaround on an escalated hold is not modeled** — under `timer_escalated` a large claim is pulled to a person at the timer but still completes the full loop, because manual claim-review turnaround has no public anchor; the study says so beside the chart (for large claims the timer changes who decides, not how long). | Phase 8b (2026-09-06) | A citable public benchmark on manual claim-review turnaround: add an `escalation_days` parameter and a fourth hold branch (a design change with its spec amendment naming the invariant it restores) |
| ~~**The `architecture-fit` skill's shape sentence names only `FORMULAS`** — the skill says "a formula is one entry in `models/cost_model.py::FORMULAS`", but Phase 8b added `models/guardrail_sim.py::RULES` as a second instance of the same shape; a skill is tooling and must not be edited inside a phase diff.~~ DONE `tooling/implementation-loop` (2026-09-07) — the sentence names both instances. | Phase 8b (2026-09-06) | The next `tooling/` branch: widen the shape sentence to name `RULES` alongside `FORMULAS`, then strike |
| ~~**No test that a Phase 9 SQL aggregate over `guardrail_sim` reproduces `summarize`** — `models/guardrail_sim.py::summarize` is the one aggregation site (mean hold days, timer-released share per scenario); Phase 9's Beat 4 panel reads the mart through SQL `avg`/`count`, rounded to the same units (`days`, `rate`), and must reproduce it.~~ DONE Phase 9d (2026-09-09) — B4.3 reads the mart through `panels._hold_summary`'s `avg`/`count` group-by (the `case` conditional count, portable); `tests/test_beat4.py::test_b43_sql_aggregate_equals_summarize` pins that reduction, rounded at the model's one site, equal to `summarize` on all four scenarios. | Phase 8b (2026-09-06) | Phase 9 renders the Beat 4 panel: add a test that the SQL aggregate over `guardrail_sim`, rounded to the same units, equals `summarize`'s output |
| ~~**The published study's corpus render is not decided** — the committed page renders over the frozen synthetic input, so B2.2/B2.4/B2.5 and B5.2 (Phase 9e) show the fixture state; a page with counted shares needs a captured render (`write(db=…)` over `ROWS=captured`), which the corpus gate allows but Phase 9b never commits. Such a page raises a brand-address question (a captured render's marts carry no per-review address, but a future source might) and a page-size question. (B2.4's drill was settled at the 9b exit round: the footer names the answer key as a repository file.)~~ DONE Phase 9f (2026-09-10) — decided: the permanent artifact stays the byte-checked **synthetic** render; a captured render is the reader's own `make rebuild ROWS=captured && make study`, named in the README. This keeps determinism and the CI byte-check, answers the brand-address question (the shipped page carries no per-review address) and the page-size question (moot — the shipped page is unchanged); no Threat-model row is needed since 9f ships no captured render. | Phase 9b (2026-09-08, pinned decision 1) | Phase 9f decides whether the published study is a captured render and, if so, adds its Threat-model row, and answers the brand-address and page-size questions (DONE above) |
| **A per-review drill needs a per-review public address** — Beat 2's trail is the mart's `reviews`/`theme_rows` counts beside each share; a row-per-review list would need a per-review public URL to be traceable and publishable, but no declared source yields one (every parser stores the brand-carrying page address — D1). | Phase 9b (2026-09-08, pinned decision 2) | A source that yields a per-review public address is declared *(Phase 9g, 2026-09-10: the review-level drill shipped over the reader's own rebuild — `study/metabase/` — but external per-review citation still needs a per-review public address no source yields; row stays open, re-deferred, with that trigger; DECISIONS → Phase 9g records the §90 deviation)* |
| **A pin asserted over the whole page pins nothing** — round 2 of Phase 9b found invariant 3's test asserting `"var(--sN)" in page`, satisfied by the fixture CSS whatever the band drew (`1b21215`); the fix asserts on the mechanism's own output (the function, the panel section's legend). `code-craft` → Test quality has no sentence on assertion scope, and a skill edit rides a `tooling/` branch, never a phase. | Phase 9b round 2 (2026-09-08, functionality-tester F1) | The next `tooling/` branch adds the sentence (assert on the smallest output that contains the mechanism's result, never the whole page or file) and the code-reviewer's test-quality pass names it; the same branch adds a fix-commit reminder for the `site-fix` grep (its mechanism, `/selfcheck`, is on request and the class recurred at 9b's exit) and gives `docs/PLAN.md` §5 the 9a–9g split the coherence-auditor file points readers at |
| ~~**The B2.2/B2.5 trail is tooltip-only** — each share's `theme_rows of reviews` counts ride in the SVG `<title>` tooltip, so a reader without hover (print, a phone, a screen reader that skips SVG titles) never sees the counts the spec calls "beside each share"; B2.4's counts are visible text. A visible denominator per x-label needs the renderer to know the denominator apart from the formatted `detail` string — a `Point` field, a design change.~~ DONE Phase 9g (2026-09-10) — the Metabase demonstration (`study/metabase/`) shows the counts as visible cells and drills to the review rows behind each theme bar over the reader's own rebuild; the visible-denominator question is answered there. The HTML page's tooltip trail is unchanged (byte-identical). | Phase 9b challenge round 2 (2026-09-08, senior-architect #7); trigger re-pointed at the Phase 9f exit (2026-09-10: the stranger test shipped and study-editor judged the README's Beat 2 prose stands without the counts, so 9f made no change — the page is byte-unchanged) | Phase 9g's Metabase review-level drill (which owns the visible-denominator question over the reader's own rebuild), or the first reader or reviewer who reports missing the counts: add the `Point` denominator field and one visible `<text>` per x-label under the chart |
| ~~**Live sliders need a script the permanent page does not carry** — Beat 3 draws each parameter's range (low, default, high) as a static mark and prints the formula beside it, so a reader redoes the arithmetic by hand; a control that recomputes `FORMULAS` in the browser is an inline script, and Phase 9a/9b settled that the permanent artifact carries none (self-contained, no CDN, byte-identical). A second copy of the formulas in a second language would also need its own module-to-script identity test.~~ DONE Phase 9g (2026-09-10) — the Metabase demonstration provides the live exploration (its own filters and the review drill) that a browser slider would have; the permanent page still carries no script (`tests/test_export.py::test_export_has_no_cdn_no_external_asset_no_timestamp`). | Phase 9c spec (pinned decision 1) | Phase 9f decided **no** inline script — the permanent page carries none (DECISIONS Phase 9f; pinned by `tests/test_export.py::test_export_has_no_cdn_no_external_asset_no_timestamp`). This row closes at the Metabase demonstration (9g), which provides the live exploration a script would have; if instead a later phase adds an inline CDN-free `FORMULAS` recompute, its pinning test is the module ↔ script identity (every expression and value at the defaults equal) |
| **B4.3's four bar labels are a latent overflow** — the B4.3 grouped bar draws one bar per simulator scenario labelled by the full display name ("A clock on every hold", 21 chars) in a legend-less four-bar band; it fits the current width but has no short-label/tooltip fallback like B1.3/B2.3's `_profile_short`, so a longer scenario name (or a fifth) would crowd. | Phase 9d review round 2 (2026-09-09, code-reviewer #6) | A Beat 4 scenario display name grows past the bar band (or a scenario is added): give `_render_grouped_bar` short axis labels with the full name in the `<title>`, the `_profile_short` pattern |
| ~~**The footer verb "opens to" fits an address, not a repository file** — the shared `_drill` (`study/export.py`) joins http links and "the repository file X" after one verb, "opens to", so a file reads as if it were a clickable target (Beat 3: "opens to `<addr>`, the repository file …csv"; 9b's B2.4: "opens to the repository file …").~~ DONE `fix/drill-footer-verb` (2026-09-09) — `_drill` builds two clauses: "opens to `<addr>`" (http links and the brief §6 citation, under the fixture verb) and a separate "source file `<path>`" (repository files, never "opened to"), joined by "; "; B2.4, B3.1–B3.4 re-rendered, `tests/test_export.py` and `tests/test_beat3.py` assertions updated. | Review round 1, Phase 9c (code-reviewer #8) | A dedicated `fix/` PR off main (or the next change to `_drill`): separate the address part ("opens to `<addr>`") from the file part ("source file `<path>`"), update B2.4's assertion and re-render both pages in that one PR |
| **`pipeline/build.py` carries raw→staging→marts alongside the readers and every Python-fed mart writer** — at ~1530 lines the one module now holds the CSV/snapshot readers, the raw-declaration guard, the review load, the relocated `reviews_per_month` query (Phase 9e), and the mart writers (model, simulator, theme-share, `classifier_quality` and the two Beat 5 marts) beside `rebuild()`/`idempotency_check()`/`reset()`. Accepted: every piece is imported and pinned, so a split is cosmetic re-homing with no behavior change; nothing is broken while the module stays whole. | Exit audit, Phase 9e (coherence-auditor tidy candidate, 2026-09-10) | A dedicated `pipeline/` cleanup PR, or the next writer added to `build.py`: move the mart writers (or the snapshot readers) into their own module under `pipeline/`, imports re-pointed, no behavior change — or record why the module stays whole |
| **Two read-figure rows sit under `Parameter`, whose docstring says "an assumption on a slider"** — `emp_p50` (8a) and `emp_mean` (9h) are figures read off the fit artifact with `low == default == high`, drawn as fixed marks (`FIXED_RANGE`); they carry the `Parameter` shape and the `sourced` sourcing because two rows are the `emp_p50` precedent, not a shape of their own. | Phase 9h challenge round 1 (2026-09-11, senior-architect #10) | Phase 9i added a third row that is not an assumption on a slider — `extra_billing_share`, a spread across the four profession families, not a fixed mark — and named the four range kinds in B3.3's blurb and note instead of a shape; a fourth such row, a reader confusing a spread with an explored range, or a reviewer asking that a context row carry a tag of its own (9i review round 1 kept the mart's Modeled, the glossary's "arithmetic we print in full" — a per-row tag is a write-path change), opens the shape: a closed third word beside `sourced`/`unsourced` or a `ReadFigure` row type, with its tag, pinned by the display split test |
| **B2.1's paraphrases render in Metabase only, not in the HTML export** — Phase 9g curated the five theme paraphrases (`study/paraphrases.yaml`, B2.1 Documented) and shows them in the Metabase drill; the permanent HTML page has no B2.1 panel (its Beat 2 renders B2.2–B2.5), so a reader of the page alone does not see the taxonomy's worked examples. | Phase 9g (2026-09-10) | A later `study/` render pass adds a B2.1 panel (the taxonomy card: paraphrase + source per theme), or the study records that B2.1 ships Metabase-only by decision |
| **The claim-cost anchor is the public insurer's reimbursement; a complementary insurer's share of the same bill has no anchor** — Beat 3's mean claim is the DAMIR reimbursed amount per cell (the Assurance Maladie's part of a bill); the part a complementary insurer refunds — the unreimbursed share of the tariff plus the extra billing — has no distribution of its own, only the 9i share saying how large the extra-billing part is nationally. | Phase 9i (2026-09-12) | A reader or reviewer asks for the complementary-side claim size or share: DAMIR's fee (`PRS_PAI_MNT`) and base (`PRS_REM_BSE`) columns can supply both per cell, at a `fixtures/damir/` re-freeze (a DECISIONS entry), read the same closed-form way |
| **Every `csv` reader but one maps `ValueError` only, and the DAMIR sample path has no boundary catch at all** — `csv.Error` (a field past the parser's limit) is no `ValueError`, so it escapes `opendata/slice.py:118` (and `_do_sample_damir` wraps that read in no `try` at all), `opendata/fit.py::read_name_value_rows` behind `build.py`'s `(ValueError, OSError)`, `pipeline/build.py::_read_csv` and `read_fixture`, `classify/cache.py`, `classify/eval/labels_io.py` and `ingest/trustpilot.py::parse` as a traceback, not the one-line refusal each boundary promises; only `opendata/fee_split.py::_iter_rows` folds it (9i round 2). Pre-9i code, so not a fix in the 9i diff. | Phase 9i review round 2 (2026-09-12, security-reviewer #1, the `traceback-at-boundary` class) | A `fix/` PR, or the next change to any reader listed: each reader folds `csv.Error` into the refusal it declares (the 9i `fee_split` shape — the reader owns its failure type) and `_do_sample_damir` gains the boundary catch, pinned by one test that walks every `csv` reader with a field past the limit and asserts a declared refusal, never `csv.Error` |
| ~~**Two tooling texts lag the code** — `.claude/skills/secure-by-construction/SKILL.md` says "`data/*` minus the two tracked subtrees" where `.gitignore` and CLAUDE.md track three (`snapshots/`, `damir/`, `ameli/`); and `code-craft` → Error policy does not yet say that a reader owns its failure type (folds a parser's own error into the refusal it declares, so the boundary catches one declared set) — the `traceback-at-boundary` class hit twice in Phase 9i. A skill edit rides a `tooling/` branch, never a phase.~~ DONE `tooling/skill-sentences` (2026-09-12) — secure-by-construction names the three subtrees and the third tracked output (`data/ameli/fee_split.csv`); code-craft → Error policy gains the reader-owns-its-failure-type bullet citing `decode_json` and `fee_split._iter_rows`; the LESSONS `traceback-at-boundary` Status names it. | Phase 9i review round 2 (2026-09-12, coherence-auditor #8; LESSONS `traceback-at-boundary`) | The next `tooling/` branch: both sentences land, and the LESSONS row's Status names the second |
